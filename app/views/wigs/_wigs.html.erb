<div id="<%= dom_id wig %>">

  <%# div da imagem %>
  <div class="container-show d-flex justify-content-between">
    <div class="col-4">
      <%= cl_image_tag wig.photo.key, height: 400, width: 300, class: "img-wig" %>
    </div>

    <%# div das informacoes %>
    <div class="col-8">
      <p> <span class="h2"><strong><%= wig.name %></strong></span> </p>

      <%# div ad descriçao %>
      <div id="description">
        <p> <h4><strong>Description:</strong></h4> </p>
        <p> <%= wig.description %> </p>
      </div>

      <%# div das caracteristicas %>
      <div class= "wig-features">
        <div id="type-color">

          <div>
            <p> <span class="h4"><strong> Wig Type: </strong></span><%= wig.wig_type %> </p>
          </div>

          <div>
            <p> <span class="h4"><strong>Color: </strong></span><%= wig.color %> </p>
          </div>

        </div>

        <div id= "owner-price">
          <div>
            <p> <span class="h4"><strong>Seller: </strong></span><%= wig.user.email %> </p>
          </div>

          <div>
            <p> <span class="h4"><strong>Price:</strong></span> <%= wig.price %> € </p>
          </div>

            <%# <% if policy_scope (@wig).edit? %>
            <%# <div class="edit-delete">
              <%= render "/shared/buy_button", wig: @wig, selling: @selling%>
            <%# </div>
          </div>
          </div>
              <p> <%= link_to "Back", wigs_path, class: "back-btn" %> <%# </p> %>



        </div>
      </div>


      <% unless policy(wig).edit? %>
        <%= render "/shared/buy_button", wig: wig, selling: @selling, class: "edit-delete" %>
      <%# <% else %>
      <div class="edit-remove">


         <%# <%= link_to "DELETE", wig_path(wig), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this wig?"}, class: "btn-edit" %>

         <%# <%= link_to "EDIT", edit_wig_path(wig), class: "btn-edit" %>

          <%# <%= link_to "delete", delete_wig_path(@wig) %> 

      <% end %>
      </div>


    </div>
  </div>
</div>
